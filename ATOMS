#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		float n,k,m;
		cin>>n>>k>>m;
		if(m<=n)
		{
			cout<<"0"<<endl;
			continue;
		}
		float x=log(m/n)/log(k);
		int t=floor(x);
		cout<<t<<endl;
	}
}