#include <iostream>
#include<algorithm>
using namespace std;

int main()
{
	int t;
	cin>>t;
	
	for(int i=1;i<=t;i++)
	{
		int m,n;
		cin>>m>>n;
		int a[m],b[n];
		int max1=0,max2=0;
		for(int i=0;i<m;i++)
		{
			cin>>a[i];
			if(a[i]>max1)
			max1=a[i];
		}
		for(int i=0;i<n;i++)
		{
			cin>>b[i];
			if(b[i]>max2)
			max2=b[i];
		}
		if(max1>=max2)
		cout<<"Godzilla"<<endl;
		else
		cout<<"MechaGodzilla"<<endl;
	}
} 