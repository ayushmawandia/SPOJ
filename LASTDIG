#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int a;
	cin>>a;
	while(a--)
	{
		int a;
		long long int b;
		cin>>a>>b;
		a=a%10;
		if(b==0)
		{
			cout<<"1"<<endl;
			continue;
		}
		b=b%4;
		if(b==0)
		{
			b=4;
		}
		a=pow(a,b);
		a=a%10;
		cout<<a<<endl;	
	
	}
	return 0;
}